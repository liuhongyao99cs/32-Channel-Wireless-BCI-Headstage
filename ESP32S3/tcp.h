#ifndef TCP_H
#define TCP_H

////////////////////////////////////////////////////////////////

bool initTCP();
int sendtcp(void*, int, int);
bool sendToTcp(char*, int);
bool recvFromTcp(char*, int);

////////////////////////////////////////////////////////////////

#endif /* TCP_H */